const initalState = {
  username: '',
  id: '',
  profile_pic: ''
}

export default function reducer(state = initalState, action){
  switch (action.type){
    default:
      return state
  }
}

export function getUserInfo (){
  
} 
